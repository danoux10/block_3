<fieldset class="section-form" id="inventory->{{ form_method }}">
  <legend>{{ page_name }} {{ type_form }}</legend>
  {{ form_start(form_inventory) }}
    {{ form_widget(form_inventory) }}
    {% include'controller/form/utility/_form_submit_required.html.twig' %}
  {{ form_end(form_inventory) }}
</fieldset>